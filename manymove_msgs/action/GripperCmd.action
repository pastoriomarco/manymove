# Goal
float64 position        # Desired gripper position (0.0 = closed, 1.0 = open)
float64 max_effort      # Maximum effort (set to 0.0 if unused)

---
# Result
bool success                # True if the command executed successfully
float64 current_position    # Current gripper position
string message              # Optional result message

---
# Feedback
float64 current_position  # Current gripper position